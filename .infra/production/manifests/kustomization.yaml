apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gio-site

resources:
  - namespace.yaml
  - deployment.yaml
  - ingress.yaml
  - certificate.yaml

commonLabels:
  app.kubernetes.io/part-of: gio-site
