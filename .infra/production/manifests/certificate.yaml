apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gio-site-tls
spec:
  secretName: gio-site-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - gio.pet
    - www.gio.pet
